<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:jaxws="http://cxf.apache.org/jaxws"
	xsi:schemaLocation="
http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
http://cxf.apache.org/jaxws http://cxf.apache.org/schemas/jaxws.xsd">

	<import resource="classpath:META-INF/cxf/cxf.xml" />
	<import resource="classpath:META-INF/cxf/cxf-extension-soap.xml" />
	<import resource="classpath:META-INF/cxf/cxf-servlet.xml" />

	<bean id="propertyPlaceholderConfigurer"
		class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
		<!-- Set how to check system properties: as fallback, as override, or never. 
			The default is "fallback": If not being able to resolve a placeholder with 
			the specified properties, a system property will be tried. "override" will 
			check for a system property first, before trying the specified properties. 
			"never" will not check system properties at all. -->
		<!-- 系统属性总是优先, 即: JVM "-Dkey=value"方式设置的属性信息总是优先于在配置文件的设置. -->
		<property name="systemPropertiesModeName" value="SYSTEM_PROPERTIES_MODE_OVERRIDE" />
		<property name="ignoreResourceNotFound" value="true"></property>

		<property name="locations">
			<list>
				<value>classpath:pim-config.xml</value>
				<value>classpath:test/pim-config.xml</value>
				<value>file:${user.home.dir}/pim-config.xml</value>
			</list>
		</property>
	</bean>

	<bean id="requestManager" class="com.ibm.tivoli.pim.service.SimpleRequestManagerImpl">
		<property name="pimAccountProfileName" value="PIMProfileAccount" />
		<property name="platformContext">
			<bean class="com.ibm.itim.apps.InitialPlatformContext">
				<constructor-arg>
					<props>
						<prop key="enrole.platform.contextFactory">com.ibm.itim.apps.impl.websphere.WebSpherePlatformContextFactory</prop>
            <prop key="enrole.appServer.url">iiop://iam:2809</prop>
            <prop key="enrole.appServer.ejbuser.principal">administrator</prop>
            <prop key="enrole.appServer.ejbuser.credentials">smartway</prop>
					</props>
				</constructor-arg>
			</bean>
		</property>
	</bean>
	<jaxws:endpoint id="requestManagerService" implementor="#requestManager"
		address="/requestManager" serviceName="RequestManager" />

</beans>

